<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
 
<hibernate-configuration>

<session-factory>

    <!-- SQL dialect -->
    <!-- <property name="dialect">org.hibernate.dialect.H2Dialect</property> -->

    <!-- Database connection settings -->

    <property name="hibernate.connection.driver_class">org.postgresql.Driver</property>

    <property name="connection.url">jdbc:postgresql://localhost:5432/hibernate-test</property>
    <property name="connection.username">user</property>
    <property name="connection.password">password</property>

    <!-- JDBC connection pool (use the built-in) -->
    <property name="connection.pool_size">1</property>


    <!-- Disable the second-level cache -->
    <property name="cache.provider_class">org.hibernate.cache.internal.NoCacheProvider</property>

    <!-- Echo all executed SQL to stdout -->
    <property name="show_sql">false</property>

    <!-- Drop and re-create the database schema on startup -->
    <property name="hbm2ddl.auto">update</property>
    <!-- <property name="hbm2ddl.auto">validate</property> -->
	
    <property name="hibernate.jdbc.lob.non_contextual_creation">true</property>
	

   <!-- The mapping information of entities -->
    
	<mapping class="hibernate.simple.examples.entity.Role" /> 
	<mapping class="hibernate.simple.examples.entity.UniUser" /> 
	<mapping class="hibernate.simple.examples.entity.Professor" /> 
	<mapping class="hibernate.simple.examples.entity.Course" /> 
    <mapping class="hibernate.simple.examples.entity.Student" />
	<mapping class="hibernate.simple.examples.entity.Employee" />
	

</session-factory>
</hibernate-configuration>
